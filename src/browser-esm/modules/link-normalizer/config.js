export const config = {
  title: 'Markdown Link Normalizer',
  placeholder: 'textを入力してください',
  defaultText: `Processing Rules:
1. For [[A|B]] pattern → Keep only A (display text)
2. For [[A]] pattern → Keep only A (link content)`,
  buttonText: 'Process Text',
}
